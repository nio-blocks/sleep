{
    "nio/Sleep": {
        "Properties": {
            "interval": {
                "description": "How long to wait before notifying signals"
            }
        },
        "Version": "1.0.0",
        "Output": {
            "Description": "The same signals, but only output after a delay"
        },
        "Commands": {},
        "Description": "Sleep for a given interval before passing through the signals",
        "Category": "PathModifier",
        "Input": {
            "Description": "Any signals"
        }
    }
}